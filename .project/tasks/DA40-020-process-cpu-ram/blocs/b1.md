# B1 — Schéma et échantillon Core

- `MakeDev.Status` : `cpuPercent` (fini ≥ 0) et `rssBytes` optionnels.
- `sumProcessTree` : racine + descendants, RSS Ko × 1024. PID étranger exclu.
- Marche `pgrep -P` puis `ps -p`, plafond 8 niveaux / 64 PID. Échec ou plafond → champs absents.
- `start` relit `status`, donc la réponse ON porte l’échantillon. `stop` retire le handle.

Checks : `packages/core` `bun test test/make-dev.test.ts` — 8 pass. `packages/schema` `bun typecheck` — pass.
