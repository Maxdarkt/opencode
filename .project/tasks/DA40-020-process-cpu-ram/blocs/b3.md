# B3 — Affichage et poll

- `cpuRam` : OFF → `—` ; ON sans les deux champs → `unknown` ; ON → `n% · n Mo`.
- Poll GET `/api/make-dev` toutes les 2 s seulement si `state === "on"`.

Checks : test `session-make-dev.test.ts` — 5 pass. `packages/app` `bun typecheck` — pass. `git diff --check` — pass.
