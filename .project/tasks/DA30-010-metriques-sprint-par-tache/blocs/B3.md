# B3 — Tests Core A/B + régressions

## Résultat

`packages/core/test/task-metrics.test.ts`. Correction schéma : `TaskMetrics.Freshness` = `available | unknown` (littéraux) ; `toTaggedUnion` cassait si `Unavailable` union.

Cas : DA30-007 coût `0` → `unknown` ; sprint incomplet + doublon ; A/B mesurées distinctes, pas d’attention sprint, totaux `measured` seulement si toutes mesurées ; file doublon → `QueueBlocked` sans totaux.

## Checks

| Check | Résultat |
|---|---|
| `bun test test/task-metrics.test.ts test/task-queue.test.ts` (`packages/core`) | PASS 10 tests |
| `bun run --cwd packages/core typecheck` | PASS |
