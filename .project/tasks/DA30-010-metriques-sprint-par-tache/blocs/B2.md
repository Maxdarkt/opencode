# B2 — Core lecture, file, honnêteté

## Résultat

`packages/core/src/task-metrics.ts` seulement.

- Par tâche : métriques existantes + `freshness` `unknown` (pas d’`observedAt` d’absence) ; `attention` `AttentionFact` `unknown` ; `sources` `task_binding`. Coût toujours `unknownCost` (jamais un 0 runner).
- Sprint : `taskIDs` ordre d’entrée dédupliqué ; `tasks[]` aligné ; totaux `combine*` (inconnu non promu en `measured`) ; `freshness.available.observedAt` de l’appel ; `sources` dédupliquées des membres ; **pas** d’attention sprint.
- `queue?` : `TaskQueue.evaluate` d’abord ; `kind === "blocked"` → `QueueBlockedError` (`TaskMetrics.QueueBlocked`), aucun Sprint/total.
- `TaskOwnership` : schémas/faits (contrat inchangé). Pas `TaskOwnership.Service.read` (requiert Identity + attention caller ; graphe Database conservé).

Pas HTTP, SDK, tests (B3).

## Checks

| Check | Résultat |
|---|---|
| `bun run --cwd packages/core typecheck` | PASS |
| `git diff --check -- packages/core/src/task-metrics.ts` | PASS |

## Git

Branche `task/DA30-010-metriques-sprint`, HEAD `7df15b2cd`. Pas commit/push/merge.
