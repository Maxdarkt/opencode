# B1 — Schéma public `TaskMetrics`

## Résultat

`packages/schema/src/task-metrics.ts` seulement. Index Schema inchangé.

- `Task` : `freshness` (`available` + `TaskOwnership.Freshness` **ou** `TaskOwnership.Unavailable`, dont `unknown`) ; `attention` = `TaskOwnership.AttentionFact` (déjà available/unavailable, jamais un zéro) ; `sources: TaskOwnership.Provenance[]`. `provenance: string[]` conservé sur Numeric/Tokens/Models.
- `Sprint` : mêmes `freshness` et `sources`. **Pas** d’attention agrégée.
- `Request.sprint.queue?` : `TaskQueue.Entry[]` (entrée de `evaluate` ; `TaskQueue.Request` n’existe pas).

Pas Core, HTTP, SDK, generate.

## Checks

| Check | Résultat |
|---|---|
| `bun run --cwd packages/schema typecheck` | PASS (`tsgo --noEmit`) |
| `git diff --check -- packages/schema/src/task-metrics.ts` | PASS |
| `bun install --frozen-lockfile --ignore-scripts` | runtime local pour tsgo ; lockfile inchangé |

## Limites

Core `Task.make` / `Sprint.make` incomplets jusqu’à B2 (champs requis). Pas un check B1.

## Git

Branche `task/DA30-010-metriques-sprint`, HEAD `7df15b2cd`. Pas commit/push/merge.
