# B4 — HTTP + SDK

## Résultat

`POST /global/metrics` inchangé, contrat étendu. Handler passe `queue?`. Erreur `QueueBlockedError` (`httpApiStatus` 409). Tests HTTP A/B + file bloquée sans totaux.

Generate : `bun run --cwd packages/client generate` (pas de diff client). SDK JS `./packages/sdk/js/script/build.ts` → `packages/sdk/js/src/v2/gen/types.gen.ts`.

## Checks

| Check | Résultat |
|---|---|
| `bun test test/server/httpapi-global.test.ts` (`packages/opencode`) | PASS 9 tests |
| typecheck opencode, client, sdk/js | PASS |
| `git diff --check` ciblé | PASS |
