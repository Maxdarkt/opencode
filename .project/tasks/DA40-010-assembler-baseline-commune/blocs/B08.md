# B08 — Générations et Verify

- Statut : terminé.
- Dépendances : `bun install --frozen-lockfile` exit 0, 4687 packages ; aucun lockfile modifié.
- SDK : `./packages/sdk/js/script/build.ts` exit 0 ; les deux fichiers générés conservent les blobs `524e3c...` et `577d141...`, identiques à DA10.
- Tests ciblés : Core 7/7, HTTP/OpenAPI 26/26, SDK 1/1, app 36/36, soit 70/70.
- HTTP complet : `bun run test:httpapi` exit 0 ; coverage, auth et effect terminent chacun à 209 pass, 0 fail, 0 skip, 0 missing, 0 extra.
- Typechecks : Schema, Core, opencode, SDK et app exit 0 ; `make typecheck` exit 0.
- Format/whitespace : `make format` et `git diff --check` exit 0.
- Lint ciblé : 31 fichiers TS/TSX intégrés, 0 erreur et 187 warnings.
- Baseline globale : `bun run lint` retrouve 4900 warnings et 1 erreur dans `packages/session-ui/src/v2/components/prompt-input/index.tsx`, blob inchangé depuis `702bf7dcd`. `bun run test:unit` app donne 736 pass/1 fail sur `pa-PK`; test et implémentation concernés sont inchangés depuis la base.
- Conclusion : aucun échec nouveau imputable à la candidate intégrée ; les deux échecs globaux restent dette héritée documentée en B09.
