# Runbook de diagnostic et reprise

Racine de toutes les commandes : /Users/leanbot/Documents/40_Daidalon/features/30-agent-runtime, ou paquet descendant explicitement sélectionné. Branche 30-agent-runtime ; HEAD du manifest. Aucun serveur à relancer.

1. Relire STATE/scope/plan, AGENTS et .project/apex.json, le mandat canonique Sprint 1 et le handoff DA40-003. Vérifier MT, pwd/top-level/branche/HEAD/dirty. Les dossiers M0, preuves et fichiers untracked sont intentionnels.
2. Lire manifest.json et comparer les SHA256 dans evidence/source-sha256.json. L’instance live appartient au worktree 40, pas au worktree 30. Relever les cwd/listeners actuels avant d’utiliser les PID historiques ; aucun kill ni restart.
3. GET http://127.0.0.1:4140/global/health : healthy:true sélectionne V1 selon server-protocol.ts. GET UI4440 démontre disponibilité seulement. En cas de processus absent ou de source différente, consigner la divergence ; demander au parent un environnement attribué, sans réutiliser aveuglément les ports.
4. Lire la route dans diagnostic-runtime.md ; vérifier la sélection protocole et le placement persisté de session avant toute expérimentation d’écriture. Ne pas envoyer de prompt sur l’instance DA40 réservée à une recette sans fournisseur.
5. Rejouer les tests depuis ce worktree avec `python3 .project/tasks/DA30-003-qualification-parcours-runtime/evidence/run-checks.py`. Le runner utilise un cwd explicite par paquet, un environnement allowlist sans clés, RECORD=false, HOME/XDG/TMPDIR sous evidence/runtime. Les fixtures temporaires sont gérées par les tests ; les preuves et l’héritage restent conservés. Le runner est un artefact de recette, pas une modification produit.
6. Prérequis reproductible : Bun1.3.14 copié depuis le runtime DA40 accepté, SHA256 e0c90ec15d33363e6b70713d56bc3b2c7585c17f40a0fe0f8fd9305901d4e233. Dépendances installées dans le worktree30 via `evidence/runtime/bun install --frozen-lockfile --ignore-scripts` (chemin relatif au dossier tâche pour le binaire). 4687 paquets ; exit0. Aucun node_modules partagé, script prepare/husky ni lockfile changé. Si runtime manque, appliquer procédure DA40, jamais upgrade global.
7. Reprise corrective : ouvrir un nouveau bloc B04 dans ce dossier après mandat parent, borner le défaut documentaire/test, relire les preuves concernées et rejouer uniquement les checks affectés. Garder review ou repasser in_progress selon la correction avec MT relu ; jamais done/archived enfant.
