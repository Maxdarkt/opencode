# Matrice des contrôles de placement

Les références visent HEAD du manifest ; extraits numérotés conservés dans evidence/source-lines.txt. « Démontré » signifie la portée nommée, jamais une certification de tous les écrivains.

| Famille | Point démontré / preuve | Limitation | Verdict Sprint 2 |
|---|---|---|---|
| Admission V1 pilote | Handler session.ts:311 vérifie session puis SessionPrompt ; routage relit placement persisté ; fixtures client-route | Pas d’inbox V2 déduite du 204 ni de admittedSeq:0 compatible. Aucune garde tâche/lease | Conditionnel : garde avant admission et avant lancement, dédiée V1 |
| Admission V2 | session.ts:360, SessionInput.admit ; 41 tests admission/location/coordinator | Résume par wake après commit ; coordination par Session ID dans un processus, pas par worktree interprocessus | Faisable comme point d’insertion, couverture lease à construire |
| Placement V2 | SessionLocationMiddleware + execution/local.ts + runner/llm.ts:179 | directory/workspace comparés, sans génération de binding ni état Git ; course après vérification possible | Conditionnel : revalidation au lancement et effets |
| write/edit/apply_patch V2 | LocationMutation.resolve canonise existant/ancêtre, rejette relative_escape/location_escape ; PermissionV2.assert edit et external_directory ; tests feuilles | Autorisation externe possible ; pas de sandbox OS. FileMutation sérialise par cible dans son service ; ne clôt pas les écrivains externes ni toutes courses symlink entre résolution et écriture | Faisable pour feuilles coopérantes, conditionnel pour protection complète |
| Concurrence fichiers | FileMutation.writeIfUnchanged compare sous verrou local ; test Edit rejette changement après matching | Pas de verrou global sur le worktree, pas de fence reconnue par éditeur externe | Conditionnel |
| write/edit/apply_patch V1 | opencode/src/tool/write.ts:44-64 ; edit.ts:83-155 ; apply_patch.ts:74,206-239 : demandes edit/external_directory avant écritures | Points différents de V2 ; cette campagne ne rejoue pas toutes les feuilles historiques | Conditionnel ; inventaire V1 obligatoire si route conservée |
| Shell V2 | tool/bash.ts:129-170 : résolution cwd, external_directory, permission commande, AppProcess.run ; tests injectent AppProcess et permissions | Scanner d’arguments consultatif ; redirections/interpréteurs/réseau/code dynamique gardent autorité de l’utilisateur hôte. Tests n’exécutent pas les descendants OS | Non faisable comme confinement universel ; lancement coopératif conditionnel |
| Shell V1 / shell direct | tool/shell.ts:263-305,484,612-636 ; SessionPrompt.shell via startShell | Une garde uniquement sur le prompt normal manquerait la route shell directe ; même limite OS | Conditionnel avec gate de lancement commune, sinon refus |
| PTY V1/V2 | handler historique utilise Core Pty ; core/pty.ts:165-183 accepte cwd ou Location.directory puis spawn ; écriture stdin :256/290 | Le service ne passe pas par LocationMutation/PermissionV2 avant spawn ; session PTY distincte, autorité shell ; ticket websocket ne prouve pas un lease écrivain | Conditionnel : contrôler create ET stdin et cycle complet, ou interdire PTY pour écrivain géré |
| Descendants de processus | cross-spawn-spawner.ts:292-310,384-435 : groupe POSIX / taskkill Windows, fallback killOne, escalade SIGKILL | Sémantique de nettoyage présente, mais pas preuve des descendants échappés/daemonisés ou effets distants ; process group ≠ containment. Pas de test OS/Windows dans cette campagne | Quiescence inconnue ⇒ refus de transfert, même après expiration |
| Outils applicatifs et plugins/MCP | ToolRegistry.settle :50-70 capture une feuille ; AGENTS outils dit autorisation à la feuille et lacunes d’enregistrement MCP/plugin | Visibilité du catalogue ≠ autorisation ; une feuille peut écrire via API directe, processus ou réseau. Pas de garantie d’interception globale | Refus par défaut si capacité non déclarée et non contrôlée |
| Outils exécutés fournisseur | runner/llm.ts:253 ignore providerExecuted au règlement local | Aucun contrôle filesystem local ne prouve fin/compensation d’un effet distant | Hors preuve ; inventaire et protocole de fin requis |
| Écritures internes (DB, snapshots, sortie outil, config/plugins) | SessionInput persiste ; ToolRegistry resources.bound ; FileMutation couvre ses propres appels | Un hook sur les seuls outils edit ne couvre pas ces services ni tous les accès FS directs | Classer répertoires d’état séparément, inventorier avant prétention universelle |

Aucune implémentation ni dette de code introduite. Qualification OS/PTY, scénarios multiprocessus, symlink adversarial et effets distants restent explicitement non réalisés. Le dossier ne transforme aucun timeout ni signal d’arrêt en certificat de quiescence.
