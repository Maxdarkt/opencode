# B1 — Contrat App chrome 0.3 (tests existants)

## Fait

Rejoué, pas dupliqué. `node_modules` absent du worktree neuf →
`bun install --frozen-lockfile` (18 extraits, `bun.lock` inchangé).

24 tests verts :

- `sessionWorkbenchLayout` : 1440 rail 244 + chat + tray, secondaire
  optionnel ; 1024 rail 64, split encore adressable ; largeur secondaire
  clamp 280, pas de reuse `session width`.
- `secondary tabs` : fermé par défaut ; 1er open → Files ; `+` focus
  sans doublon ; dernier close ferme le panneau.
- Files / Diff / Browser : arbre **à droite** ; crumb observé ;
  fail-closed ; diffs vides honnêtes ; Browser `about:blank` sans iframe.
- `packInspectorView` : tokens / coût `unknown` (jamais `0` / `$0`) ;
  git sans HEAD inventé ; merge = simulation.

Aucun fichier produit. Pas de trou 0.3 démontré.

## Checks

- `bun test --conditions=solid --preload ./happydom.ts` (3 fichiers) : 24 pass
- `bun run typecheck` (`packages/app`, `tsgo -b`) : PASS
- `git diff --check` : PASS (vide hors APEX)
- `bun.lock` : inchangé

## Suite

B2 — Maquette servie 1440 / 1024.
