# B3 — Écarts + isolation A/B

## Fait

`ecarts.md` : attendus (bandeau ☰+split, Contexte, Browser stub, App
honnête, `/sprint/cockpit` inchangé) vs grille 0.3 **nulle** aux deux
largeurs. Pas de C1. Fixtures `DA40-015-A/B` / `sprint-cockpit-input.ts`
non touchés.

Isolation A/B : `sprint-cockpit.test.ts` 8 pass (A/B distincts, pas de
zéro inventé, launch via open, merge inerte).

E2E Playwright : Chromium **absent** (`ms-playwright` cache vide) → limite
notée, pas de téléchargement.

Session live : non inventée (voir Smoke).

## Checks

- `bun test src/pages/sprint-cockpit.test.ts` (`packages/app`) : 8 pass
- `ecarts.md` complet : PASS
- E2E : skip (Chromium absent)
- C1 : non
- `git diff --check` pathset : PASS (APEX seulement)
- `bun.lock` / fixtures A/B : inchangés

## Suite

Smoke — maquette 8766 + App ports 18 si `config-check` / session liée.
