# B2 — Maquette servie 1440 / 1024

## Fait

`python3 -m http.server 8766 --bind 127.0.0.1 --directory docs/product/maquette`.
`http://127.0.0.1:8766/cockpit.html` (pas le fichier éditeur).

### 1440×900

| Étape | Preuve |
| --- | --- |
| 1. Chat plein | CDP `inner 1440×900`, rail **244**, secondaire fermé, tray présent. Recapture après fermeture split : mêmes mesures. |
| 1b. Split → Files | Onglet `cockpit.html`, Explorer **à droite** (`treeX 1220` > `fileX 818`), poignée `#split` visible (5px @ x 813). |
| 2. `+` | Menu Browser / Git Diff / Files. Browser → `about:blank`, **pas d’iframe**. Git Diff → crumb « Modifs de ce chat · 2 fichiers » + patch. |
| 3. ☰ | Inspecteur flottant `display:flex`. Onglets maquette : Tâche, Git, Coût, Serveurs, Permissions (pas Contexte — écart **attendu** vs App 009). Terminal bas : `$ pwd` / staging. |

### 1024×768

Rail **64**, bouton split 28×28 visible (x 986), secondaire encore ouvert, `+` présent. ☰ visible.

### Isolation HTML

Access log serveur : uniquement `GET /cockpit.html` (200, 2×). `performance` resources vide (HTML inline). Aucun `src`/`href` externe. Seule chaîne `http://127.0.0.1:4417` = **texte** barre Browser simulé, pas un fetch. **PASS**.

Fixtures `$12.40` / HEAD maquette **non** traitées comme contrat App.

Pas de trou 0.3 (la maquette **est** le référentiel). Aucun fichier produit.

## Evidence

- `evidence/1440-chat-plein.png` / `1440-files.png` : même SHA (course 1er clic split) — structure = CDP
- `evidence/1440-inspecteur-diff.png` (SHA `e14ecfe6e30b`)
- `evidence/1024-rail-compact.png` (SHA `82edee3769c8`)

## Checks

- Captures présentes : PASS
- Isolation HTML : PASS
- Grille maquette.md 1–4 : PASS

## Suite

B3 — `ecarts.md` + isolation A/B.
