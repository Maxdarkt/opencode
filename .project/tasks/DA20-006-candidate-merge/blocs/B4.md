# B4 — Checks candidate

Statut : terminé.

Typecheck PASS : schema, core, protocol, server, opencode, app.

Tests ciblés PASS :
- `packages/app` : cockpit + pack-inspector + make-dev + secondary + rail — 44 pass
- `packages/core` : `test/make-dev.test.ts` — 6 pass

`make config-check` PASS (`.make.env` ignoré 06/4106/4406). `git diff --check` vide.

Provision : `bun install --frozen-lockfile` **dans cet arbre** (le symlink staging ne voyait pas `make-dev`).

Prochaine action : Smoke.
