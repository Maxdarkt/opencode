# B3 — Garde-fous cockpit

Statut : terminé.

- Copy `sprint.cockpit.contextNote` + `confirmationDescription` : cible `staging`, refus force-push et `master`/`develop`. 62 locales.
- `confirmCockpitAction` commit/merge/production inchangé (`simulated`). `packInspectorMergeAction` inchangé.
- `sprint-cockpit-input.mergeTarget` reste `"dev"`.
- Test ajouté : copy staging / force-push / master.

Checks : `packages/app` `bun typecheck` PASS ; `bun test src/pages/sprint-cockpit.test.ts` 14 pass.

Prochaine action : B4 checks candidate.
