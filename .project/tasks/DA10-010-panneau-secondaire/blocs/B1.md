# B1 — État, icône, slot, poignée

Status: PASS

## Fait

- `layout.secondary` persisté `{ opened: false, width }`, API open/close/toggle/resize, clamp ≥ 280.
- Icône split `data-testid="session-secondary-toggle"` à droite du ☰ (layout v2 seulement).
- Slot v2 : plus de `SessionSidePanel` ; `showSecondary` = `layout.secondary.opened`.
- Chat `flex-1` ; poignée `ResizeHandle` `edge=start` min 280.
- `fileTree.toggle` v2 → `layout.secondary.openFiles()`.
- `sessionWorkbenchLayout` : 1440 fermé sans secondaire, ouvert = `showSecondary` ; 1024 rail 64.

## Checks

- `session-workbench-layout.test.ts` PASS (fermé / ouvert / 1024 / clamp ≠ session 600)
- `session-secondary.test.ts` toggle/open Files
- `bun typecheck` packages/app PASS
