# B3 — Files, Git Diff, Browser

Status: PASS

## Fait

- Files : crumb du chemin observé ; `file.load` ; FileTreeV2 colonne **droite** (`session-secondary-files-tree`) ; hors arbre / manquant → omitted / unknown.
- Git Diff : `turnDiffs()` via `secondaryDiffsFromTurn` ; `+n/−n` ; patch entier ou `unknown` ; empty honnête.
- Browser : `about:blank`, pas d’iframe ; `+ onglet` toast simulation.
- `data-testid` : `session-secondary`, `session-secondary-files-tree`, `session-secondary-diff`, `session-secondary-browser`.

## Checks

- Tests tree-right / empty diff / stub browser PASS
- `bun typecheck` packages/app PASS
