# B2 — Onglets + menu `+`

Status: PASS

## Fait

- Modèle pur `session-secondary.ts` : kinds `files` | `diff` | `browser`, add/focus/close.
- 1er open sans onglet → Files. Ajout déjà ouvert → focus. Dernier `×` → `opened: false`.
- `session-secondary-panel.tsx` : barre collée, `+`, menu `overflow:visible`.
- i18n `session.secondary.*` en + locales (parity).

## Checks

- `session-secondary.test.ts` add/focus/close/dernier close PASS
- `i18n/parity.test.ts` PASS
- `bun typecheck` packages/app PASS
