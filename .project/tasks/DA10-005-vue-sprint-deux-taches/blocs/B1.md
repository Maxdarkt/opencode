# B1 — HttpApi lecture + generate

- Status: complete
- Checks: `packages/opencode` typecheck PASS ; `httpapi-global` + `httpapi-control-plane` PASS ; `git diff --check` PASS

## Done

- `POST /global/ownership` (`TaskOwnership.Input` → `Snapshot`)
- `POST /global/topology` (`RepositoryTopology.Input` → `Snapshot`, ownership fourni)
- Nœuds `TaskOwnership` / `RepositoryTopology` dans HttpApi server
- SDK JS généré (`global.ownership`, `global.topology`)
- Tests : A/B distincts ; identité absente → `absent`/`unknown` pas `0` ; `mergeTarget` vide → `invalid` ; payload invalide → 400 ; pas d’écriture binding/execution
