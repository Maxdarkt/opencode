# B2 — Page réelle + mapper fail-closed

- Status: complete
- Checks: `packages/app` typecheck PASS ; `sprint-cockpit.test.ts` PASS

## Done

- Fichiers App extraits de `9de3b2e1c`, renommés `sprint-cockpit*`
- Route `/sprint/cockpit` dans `app.tsx` (hors `entry.tsx`)
- Entrée explicite A/B (`sprint-cockpit-input.ts`), `mergeTarget` jamais inféré
- Mapper fail-closed : `available` → valeur ; autre état → libellé + provenance
- Canvas onglets locaux ; chat deep-link seulement si `sessionID` available
- Actions sensibles : dialogue simulation
- Fallback HTTP : `inaccessible`/`unknown`, pas de faits fixture présentés comme vrais
