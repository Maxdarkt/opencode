# B1 — Contrat Schema + defaults config

Livré.

- Schema live-only `session.next.drain.ended` (`StopReason`, `DrainEnded`, pas de `durable`)
- `ConfigBounds.Info` optionnel `{ steps?, tokens?, duration_ms? }`
- `DEFAULT_STEPS = 50`, `DEFAULT_DURATION_MS = 1_800_000`

Checks : `bun run --cwd packages/schema typecheck` PASS ; `bun run --cwd packages/core typecheck` PASS ; `git diff --check` ciblé PASS.
