# B2 — Plafond tours toujours fini

Livré.

- `resolveStepLimit(agent, config) = min(agent, DEFAULT)` si `agent.steps` posé, sinon `config.bounds.steps ?? 50`
- Dernier tour : `MAX_STEPS_PROMPT` / `toolChoice: "none"` / outils vides
- Steer / queue promu reset le compteur (inchangé)

Checks : `bun test test/session-runner-bounds.test.ts test/session-runner.test.ts` depuis `packages/core` PASS (inclus dans la passe 121) ; `bun run --cwd packages/core typecheck` PASS.
