# B3 — Budget tokens + deadline drain + signal

Livré.

- Budget drain : `input + output + reasoning` après `Step.Ended` mesuré ; usage absent → pas d’arrêt ; `cost: 0` inchangé
- Deadline murale du drain (`setTimeout`, `Fiber.interrupt` du drain, même settlement qu’Interrupt)
- `session.next.drain.ended` publié seulement sur `interrupt | steps | budget | timeout` ; idle no-op

Checks : `bun test test/session-runner-bounds.test.ts test/session-runner.test.ts test/session-run-coordinator.test.ts test/context-pack.test.ts` depuis `packages/core` → 121 pass / 0 fail ; `bun run --cwd packages/core typecheck` PASS ; `git diff --check` ciblé PASS.
