# B2 — Bandeau + Interrupt

## Fait

Vue session `/{b64}/session/{id}` : bandeau 32px. Running = `busy`|`retry`,
Idle = `idle`. Outil = dernière part tool `running`, sinon `unknown`. cwd =
`session.directory` observé, sinon `unknown`. Interrupt visible seulement si
Running → `session.interrupt` (`halt` existant).

## Checks

- `tsgo -b` app : PASS
- `bun test src/components/session/agent-banner.test.ts` : 3 pass
  (idle vs running, Interrupt seulement running, outil/cwd unknown)

## Suite

B3 — inspecteur Contexte / Coût.
