# B1 — GET pack

## Fait

`ContextPack.fromSession` assemble via Core `assemble` (pathset vide OK,
tokens sans request → `unknown` sans `value`). `GET /api/session/:sessionID/pack`
(Protocol + Server) → `ContextPack.Pack`. 404 si session absente. Hors worktree
→ `omitted`. Generate client : `sessions.pack`. App n’importe pas Core.

## Checks

- `tsgo --noEmit` schema / core / protocol / server : PASS
- `bun test test/context-pack.test.ts` (core) : 8 pass
- `bun test test/server/session-pack.test.ts` (opencode) : 2 pass
- generate client : `SessionsPack*` présent
- `git diff --check` : PASS

## Suite

B2 — bandeau + Interrupt.
