# B3 — Inspecteur

## Fait

Inspecteur flottant depuis le bandeau. Onglets Contexte (worktree, mandat,
pathset, omitted, tokens before/after) et Coût. `unknown` si métrique absente
ou `cost === 0` non mesuré. Jamais `"0"` / `$0.00` faute de mesure. Pack via
`GET /api/session/:id/pack`.

## Checks

- `tsgo -b` app : PASS
- `bun test src/components/session/pack-inspector.test.ts` : 3 pass
  (`unknown` ≠ `"0"`, coût 0 → unknown, pathset vide OK)

## Suite

Smoke session liée.
