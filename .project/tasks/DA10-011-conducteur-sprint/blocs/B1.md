# B1 — Éligibilité + rail

Status: PASS

## Fait

- `mapCockpitView` consomme `ownership.result`.
- `CockpitTaskView.eligibility` : selected match → eligible ; autre id → waiting ; `blocked` → toutes blocked + reason ; `complete` / HTTP inaccessible → none.
- Badge Unique Eligible / Blocked via `cockpitEligibilityBadge` (waiting affiché Blocked).
- Item **Pilot** dans le rail cockpit, `aria-current="page"`.
- Workbench : Pilot `aria-current` si pathname = `/sprint/cockpit`.
- i18n `sprint.cockpit.rail.eligible|blocked` + parity locales.

## Checks

- mapper selected A / waiting B PASS
- queue `blocked` → 0 éligible PASS
- complete / inaccessible → none PASS
- `sessionSprintRailSelected` Pilot current seulement sur cockpit PASS
- `bun typecheck` packages/app PASS
