# B2 — Prompt collable + Launch borné

Status: PASS

## Fait

- `cockpitPromptText` : gabarit
  `Skill apex-task. Carte {id}. cwd = {worktreeLabel}. Base {head}.`
  Null si worktreeLabel ou head pas `available`. Aucun SHA inventé.
- Bouton Copier : panneau Task status + inspecteur Tâche. Clipboard
  seulement, pas de `taskChatOpen`.
- Launch : `cockpitLaunchEnabled` = eligible + worktree observé.
  Sinon bouton disabled et `confirmCockpitAction` fail-closed.

## Checks

- gabarit exact PASS
- HEAD unknown → prompt null PASS
- Launch non éligible / worktree null → 0 appel `taskChatOpen` PASS
- merge/commit restent `simulated` PASS
- `bun typecheck` packages/app PASS
