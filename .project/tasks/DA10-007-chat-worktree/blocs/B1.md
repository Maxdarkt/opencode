# B1 — Contrat + HttpApi open + generate

## Fait

`POST /global/task-chat/open` : `get(mt_task)` reprend le fil ; sinon convention
`/features/tasks/<mtTaskID>` + `LocalContext.inspect` fail-closed, `SessionV2.create({ location })`,
`TaskBinding.adopt`. Collision / arbre absent / hors convention / non-checkout → 4xx, zéro
seconde ligne binding. `TaskBinding.node` branché sur le layer HttpApi.

## Checks

- `bun run typecheck` schema + opencode : PASS
- `bun test test/server/httpapi-global.test.ts` : 20 pass (create, reopen, refus, collision, payload)
- `bun run generate` client + `packages/sdk/js/script/build.ts` : `global.taskChatOpen`
- `git diff --check` pathset B1 : PASS

## Suite

B2 — libellé W1 + launch réel.
