# B2 — Libellé W1 + launch réel

## Fait

Mapper : `worktreeLabel` = suffixe observé `features/tasks/…`, sinon `unknown` tagué.
`sessionHref` = `legacySessionHref(worktree, sessionID)` si les deux sont connus.
Rail + en-tête canvas affichent le libellé. Confirm `launch` → `taskChatOpen` puis
navigation. Commit / merge / production restent simulation.

## Checks

- `bun run typecheck` app : PASS
- `bun test src/pages/sprint-cockpit.test.ts` : 8 pass
- `git diff --check` pathset B2 : PASS
- Fixtures A/B inchangées

## Suite

Smoke `/sprint/cockpit`.
