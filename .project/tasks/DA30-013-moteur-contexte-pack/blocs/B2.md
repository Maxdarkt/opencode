# B2 — Assemblage Core

## Résultat

- `packages/core/src/context-pack.ts` : `assemble`, `boundPathset`, `promptCacheKey`.
- `packages/core/test/context-pack.test.ts` : pathset worktree ; staging/sibling/`AGENTS.md` global omis ; tokens `unknown` sans `value` ; `Token.estimate` ; clé 64 hex stable ; hash change si cwd/règles/outils changent.

Pas de runner, pas HTTP.

## Checks

| Check | Résultat |
|---|---|
| `bun test test/context-pack.test.ts` depuis `packages/core` | PASS (6) |
| `bun run typecheck` depuis `packages/core` | PASS |
| `git diff --check` ciblé | PASS |

## Git

Branche `context-pack`, HEAD `493f3aa31`. Pas commit/push/merge.
