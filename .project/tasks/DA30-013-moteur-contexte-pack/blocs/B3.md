# B3 — Prune live + clé cache runner

## Résultat

- `SessionCompaction.prune` : copies live, seuils V1, troncature 2k, `skill` protégé, pas de mutation durable.
- Runner : prune avant `llm.stream` ; `promptCacheKey` = hash cwd+règles+outils (sans ID session, sans date).
- Date enregistrée `core/volatile/date` : après env/instructions dans le baseline, updates inchangés.
- Tests runner : même location → même clé 64 hex.

## Checks

| Check | Résultat |
|---|---|
| `bun test` ciblés core (pack, compaction, runner, builtins, task-metrics) | PASS (107, 1 prune corrigé puis vert) |
| `bun run typecheck` depuis `packages/core` | PASS |
| `git diff --check` ciblé | PASS |

## Git

Branche `context-pack`, HEAD `493f3aa31`. Pas commit/push/merge.
