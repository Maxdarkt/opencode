# B1 — Schéma `ContextPack`

## Résultat

- `packages/schema/src/context-pack.ts` : `Mandate`, `Pathset` (`RelativePath[]`), `OmittedPath`, `CachePrefix` (`cwd`, `rulesHash`, `toolsIdentity`), `Pack` (mandat, worktree, pathset, omitted, `tokensBefore`/`tokensAfter` = `TaskMetrics.Tokens`).
- `packages/schema/src/index.ts` : export `ContextPack`.

Pas Core, HTTP, SDK, generate.

## Checks

| Check | Résultat |
|---|---|
| `bun run typecheck` depuis `packages/schema` | PASS (`tsgo --noEmit`) |
| `git diff --check` ciblé schema | PASS |
| `bun.lock` | restauré après install local tsgo |

## Git

Branche `context-pack`, HEAD `493f3aa31`. Pas commit/push/merge.
