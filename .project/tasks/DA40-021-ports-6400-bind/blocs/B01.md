# B01 — Registre DA

- Contrôle `node scripts/check-project-registry.mjs` : `PASS registry: 4 projets, aucune plage en collision`.
- Commit Codex `e8f573c8c10da0c0ef647ac0be5b2d839bb195b9` — `feat(registry): place Daidalon app ports on 6400`.
- `git show --stat` : `projects/registry.json` seul, 42 insertions. Le patch ne contient que `"id": "DA"`. Pas de push.
- Copie de travail restaurée. `git diff` restant sur ce fichier : suppression des `executionWorktrees` LC, hunk préexistant, non indexé.
