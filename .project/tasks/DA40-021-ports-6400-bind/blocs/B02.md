# B02 — Écoute 0.0.0.0

- `Makefile` : `HOST ?= 0.0.0.0`. Garde UI `6400 + code`, backend `6400 + code + 2`.
- `.make.env` et `.make.env.example` : fichiers réels, `WORKTREE_CODE=00`, `UI_PORT=6400`, `BACKEND_PORT=6402`, sans `HOST`. `.make.env` ignoré par Git.
- `packages/app/src/entry.tsx` : en DEV, hôte `0.0.0.0` remplacé par `location.hostname`.
- `make config-check` : exit 0.
- `DRY_RUN=1 make dev` : `--hostname 0.0.0.0 --port 6402` et `--host 0.0.0.0 --port 6400`. Pas de `127.0.0.1`.
- `bun typecheck` dans `packages/app` : exit 0 (`tsgo -b`).
- Aucun serveur lancé. `runtime_profile` reste `none`.
