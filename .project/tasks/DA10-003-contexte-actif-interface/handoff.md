# Handoff — DA10-003

La lecture `global.context` expose maintenant binding et snapshot d’exécution en lecture seule. L’UI rend les faits et bloque l’écriture hors contexte concordant. Le parent doit exécuter le smoke visuel décrit dans `smoke-report.md` avant la clôture.
