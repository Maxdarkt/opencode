# B3 — Alertes et affichage

- `bun test` budget-alert, pack-inspector, sprint-cockpit, session-message : 31 PASS
- `bun typecheck` dans `packages/app` : PASS
- `bun typecheck` dans `packages/core` : PASS

`config.bounds.sprint_tokens` est optionnel. Sans plafond ou sans
mesure, l’alerte est `unknown`. L’inspecteur et le cockpit affichent
cette alerte. Un coût 0 ne devient pas `$0.00` dans l’onglet contexte.
