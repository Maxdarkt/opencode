# B2 — TaskMetrics lit l’état

- `bun test test/task-metrics.test.ts` : 5 PASS
- `bun typecheck` dans `packages/core` : PASS

`cost: 0` sans `costState` reste `unknown` sans valeur. Un assistant
`estimated` 1.5 est `estimated`. Un sprint estimé + inconnu est
`partial`. Deux inconnus restent `unknown`.
