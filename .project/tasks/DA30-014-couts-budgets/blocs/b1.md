# B1 — Coût de step sans faux zéro

- Check schema `bun typecheck` : PASS
- Check core `bun typecheck` : PASS
- `bun test test/session-runner-cost.test.ts` : 5 PASS
- `bun test test/session-runner.test.ts` : 94 PASS
- `bun run generate` dans `packages/client` : PASS (`cost` et `costState` optionnels)
- `git diff --check` ciblé : PASS

Le step omet `cost` sans tarif. Un tarif unique non nul devient `estimated`.
`cost: 0` n’est plus publié. Le drain budget du test runner voit `cost` absent.
