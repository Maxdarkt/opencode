# B3 — Browser iframe

Status: PASS

## Fait

- `secondaryBrowserView(status)` : OFF/unknown → `about:blank` sans iframe ; ON → `http://HOST:UI_PORT/sprint/cockpit` + iframe.
- Panneau Browser : barre URL observée ; iframe seulement si ON ; hint si pas ON ; `+ onglet` toast inchangé.
- Tests iframe seulement si ON.

## Checks

- `session-secondary.test.ts` PASS
- `packages/app` typecheck PASS
