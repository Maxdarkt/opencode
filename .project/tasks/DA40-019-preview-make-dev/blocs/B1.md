# B1 — Schema, Core MakeDev, HttpApi

Status: PASS

## Fait

- Schema `MakeDev.Status` (`off` | `on` | `unknown` + ports/host/error).
- Core `parseMakeEnv` + service location-scopé : `make -C <directory> dev` en process group, Stop = ce groupe seulement. `MakeDev.node` dans `location-services`.
- HttpApi `GET/POST /api/make-dev` (+ start/stop) + handler Server. `bun run generate` client.
- httpapi-exercise : 3 routes.

## Checks

- `packages/schema` typecheck PASS
- `packages/protocol` typecheck PASS
- `packages/core` typecheck PASS
- `packages/server` typecheck PASS
- `packages/core` `bun test test/make-dev.test.ts` PASS (parse, unknown, start scoped, Stop no-op ailleurs, ports occupés → off)
