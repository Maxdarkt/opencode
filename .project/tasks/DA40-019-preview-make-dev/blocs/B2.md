# B2 — Inspecteur Serveurs

Status: PASS

## Fait

- Vue `packInspectorServersView` : unknown = Start inactif ; OFF = `—` CPU + Start ; ON = ports lus + CPU `unknown`.
- Onglet Serveurs : état, CPU/RAM, Démarrer/Arrêter (`data-testid="inspector-servers"`).
- Fetch location-scopé `/api/make-dev` (header `x-opencode-directory`) — le client App vendored n’a pas `makeDev`.
- Contexte session partagé (`MakeDevProvider`) pour B3.
- i18n `session.inspector.servers.*` dans `en.ts`, `fr.ts` et locales (parity).

## Checks

- `packages/app` typecheck PASS
- `session-make-dev.test.ts` + `parity.test.ts` PASS
