# B1 — Canal dérivé

Fonction pure `billingChannel` : `api`, `key` et `wellknown` → `api` ; `oauth` → `abo`. Un fournisseur aux deux canaux reste sans libellé (`uniqueBillingChannel`).

Aucun argument répertoire, branche ou chemin APEX. Pas de schéma.

## Contrôle

Depuis `packages/app` : `bun test src/pages/session/billing-channel.test.ts` — 6 pass, 0 fail.
