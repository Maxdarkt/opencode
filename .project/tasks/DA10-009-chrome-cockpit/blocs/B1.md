# B1 — Shell + rail

Layout v2 : `[rail | work]`, work = chat (+ side panel DA10-010 si ouvert) puis tray sous le work. Plus de terminal en colonne droite.

## Fait

- `session-workbench-layout.ts` : rail 244 (≥1025) / 64 (≤1024), chat = largeur restante, tray toujours, secondaire seulement si ouvert.
- `session-sprint-rail.ts` : Pilote et `sessionHref` absent → `/sprint/cockpit` ; `sessionHref` observé → cette session ; pastilles seulement si `isWorking` / `hasUnread`.
- `session-sprint-rail-view.tsx` : rail via `loadSprintCockpit` (pas de duplication mapper). Même basename `.ts`+`.tsx` interdit par `tsgo` (TS5056) → vue séparée.
- `session.tsx` : chrome 0.3, side panel fermé par défaut, terminal dans le tray du work.
- i18n `en.ts` + parité locales (fr : Pilote).

## Checks

- `bun test` `session-workbench-layout` `session-sprint-rail` `session-panel-layout` : PASS (7)
- `bun run typecheck` dans `packages/app` : PASS
