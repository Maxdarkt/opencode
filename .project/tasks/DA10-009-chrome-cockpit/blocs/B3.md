# B3 — Inspecteur flottant

Inspecteur `fixed` 320px z-30. Toggle ☰. Onglets Tâche · Git · Contexte · Coût · Serveurs · Permissions. Merge = toast simulation.

## Fait

- `pack-inspector.ts` : vues Tâche / Git / unknown Serveurs·Permissions ; Git lecture ; coût inchangé (`unknown` ≠ `$0`) ; merge `simulated`.
- `session-pack-inspector.tsx` : panel `fixed` ; ☰ ; `readProjectContext` + `loadSprintCockpit` ; pas d’appel mutatif Git.
- Bandeau : bouton texte remplacé par ☰ (dans l’inspecteur du banner).
- i18n onglets.

## Checks

- `bun test` `pack-inspector` (+ B1–B2) : PASS (15)
- `bun run typecheck` dans `packages/app` : PASS
