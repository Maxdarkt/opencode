# B2 — Tray terminal split

Bandeau tray Terminal · Split · note humaine. Split = 2 panes PTY. Create KO → pane `unknown`, pas de faux shell.

## Fait

- `terminal-split.ts` : 1er pane = PTY existant ou create ; 2e = create ; échec → `unknown`.
- `terminal.new()` retourne l’id (Promise) sans changer Protocol/HttpApi.
- `terminal-panel-v2.tsx` : mode split côte-à-côte ; onglets conservés si split off.
- `session.tsx` : bouton Split dans le tray du work ; ouvre le terminal si besoin.
- i18n tray.

## Checks

- `bun test` `terminal-split` (+ B1) : PASS
- `bun run typecheck` dans `packages/app` : PASS
