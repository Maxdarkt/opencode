# B03 — Convention, fichiers locaux et preuves

Statut : terminé le 2026-09-06.

- Les cinq racines ont été relues avant création : aucun `.make.env` n'existait. Les cinq fichiers ont ensuite été créés avec seulement code, ports et host 127.0.0.1 ; aucun secret n'y est inscrit.
- `.make.env` est ignoré dans le `.gitignore` versionné de `40-tooling`. Comme les autres branches n'ont pas encore cette modification, l'exclusion locale partagée `.git/info/exclude` contient aussi `.make.env`; `git check-ignore` est vert dans les cinq racines.
- La table source/10/20/30/40/réservé 50 est documentée dans `docs/product/worktrees.md`.
- Smoke final : configuration incohérente `BACKEND_PORT=1` refusée; `help`, `ports`, `context`, préflight, dry-runs `dev-app`/`dev-server`/`dev`, parsing `make -n dev`, écoute 4140/4440 absente, ignore des cinq racines et `git diff --check` verts.
- Checks : `make format` vert ; `make typecheck` vert (`packages/app`, `packages/opencode`). Le lint global est rouge hors périmètre, consigné dans `debts.md`.

Prochaine action : Smoke/Verify terminés, remettre la carte au parent en `review` sans commit ni clôture.
