# B01 — Façade et configuration

Statut : terminé le 2026-09-06.

- Ajoutés : `Makefile`, `.make.env.example` et l'entrée `.make.env` dans `.gitignore`.
- Le Makefile résout sa racine, charge exclusivement `.make.env`, exige `WORKTREE_CODE`, `BACKEND_PORT` et `UI_PORT`, valide leur forme numérique et leur relation déterministe (`4100 + code`, `4400 + code`).
- `help`, `context`, `ports` et `preflight-ports` sont présents. Le préflight emploie `lsof` seulement en lecture et refuse un listener sans l'arrêter.
- Une première expansion `make -n` a démontré qu'une sous-invocation `$(MAKE)` peut être exécutée malgré `-n`; elle a lancé Vite sur 4440. Le PID exact `8068`, créé par cette commande de validation, a été arrêté immédiatement, puis l'absence de listener a été confirmée. La façade a été corrigée pour utiliser un préflight shell en ligne et `make` littéral pour les enfants : le second `make -n` n'a lancé aucun processus et aucun listener 4140/4440 ne subsistait.

Prochaine action : B02, valider les dry-runs des commandes déléguées avec la configuration locale créée de manière contrôlée.
