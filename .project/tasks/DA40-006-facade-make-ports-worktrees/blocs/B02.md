# B02 — Commandes déléguées et orchestration

Statut : terminé le 2026-09-06.

- `install` utilise Bun avec lockfile gelé ; `typecheck` délègue explicitement à `packages/app` puis `packages/opencode`; aucune cible test racine n'est introduite.
- `lint` reste la commande root `oxlint` existante et `format` vérifie uniquement les surfaces de façade/documentation avec Prettier.
- `dev-app` injecte `VITE_OPENCODE_SERVER_HOST=127.0.0.1` et `VITE_OPENCODE_SERVER_PORT=4140`, avec `--host`, `--port` et `--strictPort`.
- `dev-server` appelle `bun run ./src/index.ts serve --hostname 127.0.0.1 --port 4140`.
- `dev` réalise un préflight unique puis lance les deux enfants dans des sous-shells; le trap ne cible que les PID de ces enfants. Les dry-runs ont exposé les trois commandes sans créer de listener.

Prochaine action : B03, vérifier l'ignore local des cinq racines, la documentation, les checks ciblés et le smoke non destructif.
