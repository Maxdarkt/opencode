# B05 — terminé, correction parent

## Cause et changement borné
Divergence confirmée : NullOr runtime correct, mais matchLegacyOpenApi applique stripOptionalNull à tous les composants, supprimant aussi les null requis. Le générateur reçoit déjà un OpenAPI incorrect.
Le composant canonique LocalContext.Info est désormais préservé dans cette transformation legacy avant normalisation du nom. Le schéma Schema et ses propriétés requises restent inchangés ; aucune propriété facultative, aucune modification manuelle de gen. Correction limitée à public.ts, son test OpenAPI et le fichier types.gen.ts régénéré officiellement.

## Régression reproduite puis résolue
b05-red-openapi.log : nouveau test échoue sur la nullabilité manquante (exit1).
b05-red-types.log : typecheck échoue TS2322, string|null incompatible avec string (exit2). Ces deux échecs sont des preuves avant correction, conservées et annotées comme attendues dans checks.json.
Test de régression vérifie les 13 propriétés requises et leurs unions null : trois chemins canoniques/persistés, git lui-même, puis neuf champs Git. Test dans opencode vérifie aussi l’assignabilité Schema.Info ↔ SDK.LocalContextInfo dans les deux sens, donc il est réellement compilé par le typecheck ; le tsconfig SDK exclut ses propres tests.

## Validation finale
Génération SDK officielle b05-sdk-generation exit0 : types.gen.ts contient les 13 unions null, sans ? ajouté. Client generate exit0 sans diff. B05 : 21 tests HTTP/OpenAPI et 2 SDK passent ; typechecks opencode et SDK exit0 ; lint 0 warning/error ; format/whitespace pass. Tests Core et autres régressions initiaux restent valides, leurs sources sont inchangées. Total cumulé distinct : 39 tests (11 Core, 26 HTTP/OpenAPI, 2 SDK).

## Candidate et préservation
Candidate totale 15 fichiers produit, diff/sha/preservation actualisés ; ancienne candidate conservée sous evidence/b04-candidate*. B05 modifie seulement un fichier de la candidate précédente (types.gen.ts), et ajoute public.ts + son test à la liste des changements. Baseline 31 fichiers : seuls STATE propre et plan modifiés, zéro inattendu ; HEAD/index/lock inchangés. Aucun serveur live, restart, commit ou appel fournisseur.

Handoff review au parent. Correction future : B06 borné après retour parent.
