# B2 — Procédure de promotion

Fichier `.project/tasks/DA20-007-candidate-integree-sprint-8/procedure-promotion.md`.

Checklist non exécutée : ranger `staging`, merger seulement cette candidate, `git push origin staging`, `worktree remove` des arbres propres. DA40-020 exclu (`.apex/` et `fixtures/`). `suivi-sprints.md` non modifié.

SHA Verify rempli : `58433ae85`.
