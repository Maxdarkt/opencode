# B2 — Open carte + pack visible

## Fait

`packages/opencode/test/server/coding-agent-recette.test.ts` : tmp convention
`…/features/tasks/DA40-017` → `POST /global/task-chat/open` → 200 + `sessionID`.
`GET /api/session/:id/pack` → `worktree` = directory observé ; tokens
`unknown` sans `value`. Write relatif `notes.md` puis pack encore honnête
(worktree inchangé, pas de tokens `0`). Pas de generate, pas de rewrite
cockpit Sprint 4.

## Checks

- `bun test test/server/coding-agent-recette.test.ts test/server/session-pack.test.ts test/server/httpapi-global.test.ts` (opencode) : 23 pass
- `bun typecheck` (opencode) : PASS
- `git diff --check` pathset B2 : PASS

## Suite

B3 — Interrupt HTTP coupe un run.
