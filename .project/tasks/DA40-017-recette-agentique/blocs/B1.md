# B1 — Isolation write vs staging

## Fait

`packages/core/test/coding-agent-isolation.test.ts` : Location = tmp
`…/features/tasks/DA40-017`. `write notes.md` seulement dans l’arbre carte.
`../escape.md` → `Unable to write` (PathError `relative_escape` mappé), aucun
fichier hors Location. Absolu sous l’empreinte `staging` + deny
`external_directory` → pas d’écriture, HEAD+porcelain identiques. `ask` ≠
sandbox OS : recette = deny + escape. Pas de rewrite `tool-write.test.ts`.

## Checks

- `bun test test/coding-agent-isolation.test.ts test/tool-write.test.ts` (core) : 10 pass
- `bun typecheck` (core) : PASS
- `git diff --check` pathset B1 : PASS
- `bun.lock` : inchangé

## Suite

B2 — Open carte + pack visible.
