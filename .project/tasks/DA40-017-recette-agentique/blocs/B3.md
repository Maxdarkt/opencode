# B3 — Interrupt HTTP coupe un run

## Fait

Même fichier recette. Idle : `POST /api/session/:id/interrupt` → 204, session
absente de `GET /api/session/active`. Busy : config v2 `providers.test` +
`TestLLMServer.hang`, prompt v2, drain actif, POST interrupt → plus busy.
Empreinte `staging` inchangée pendant le hang. Pas de clé provider, pas de
rewrite runner/coordinator. Pas de C1.

## Checks

- `bun test test/server/coding-agent-recette.test.ts` (opencode) : 3 pass
- `bun test test/session-runner.test.ts test/session-run-coordinator.test.ts` (core) : 110 pass
- `bun typecheck` core + opencode : PASS
- `git diff --check` pathset : PASS

## Suite

Smoke ports 17.
